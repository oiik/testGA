name: "Step 1"

on:
  workflow_call:

jobs:
  sleep:
    name: "sleep 5 sec"
    runs-on: ubuntu-latest
    steps:
      - name: Run sleep 5 sec
        run: sleep 5
      - name: Close another step
        uses: styfle/cancel-workflow-action
        with:
          workflow_id: "Step 2"
